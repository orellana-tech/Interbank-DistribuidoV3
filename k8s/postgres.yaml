apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: interbank
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: equipo_dev
            - name: POSTGRES_PASSWORD
              value: secreta_dev
            - name: POSTGRES_DB
              value: interbank_dev
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: interbank
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
